[uwsgi]
socket = {{ socket }}
module = {{ wsgi_module }}
env = DJANGO_SETTINGS_MODULE={{ settings_module }}
home = {{ virtualenv }}
uid = www-data
logto = {{ uwsgi_log }}
master = 1
{% if basicauth is defined and basicauth -%}
route = ^/ basicauth:{{ realm }},{{ htpasswd_file }}
{% endif -%}
